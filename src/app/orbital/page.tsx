import { OrbitalGame } from '@/components/orbital/OrbitalGame'

export const metadata = {
  title: 'Orbital · Game Arcade',
  description: 'Space gravity puzzle game. Launch probes, slingshot around planets, master orbital mechanics.',
}

export default function OrbitalPage() {
  return <OrbitalGame />
}
